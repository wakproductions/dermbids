<%= form_for(@quote_request) do |f| %>
  <% if @quote_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quote_request.errors.count, "error") %> prohibited this quote request from being saved:</h2>

      <ul>
      <% @quote_request.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :email%><br>
      <%= f.text_field :email %>
    </div>
  <div class="field">
    <%= f.label :full_name %><br>
    <%= f.text_field :full_name %>
  </div>
  <div class="field">
    <%= f.label :postal_code %><br>
    <%= f.text_field :postal_code %>
  </div>
  <div class="field">
    <%= f.label :formstack_photo_url %><br>
    <%=
        f.select(
                :formstack_photo_url,
                ['sample_tattoo_01.jpg', 'sample_tattoo_02.jpg', 'sample_tattoo_03.jpg', 'sample_tattoo_04.jpg']
        )
    %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
